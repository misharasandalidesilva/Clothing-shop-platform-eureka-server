spring:
  application:
    name: eureka-server
  config:
    import: "configserver:${CONFIG_SERVER_URL:http://config.platform:8888}"

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
